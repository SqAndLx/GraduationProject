<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.graduation.dao.TjjeMapper">
    <select id="getBzt"  resultType="com.graduation.mode.Tjje">
        SELECT
            ry.c_name name,
            count(xf.c_id) cou
        from
            purchase xf
            left join
            personnel ry
         on
            xf.c_personnelId = ry.c_id
        GROUP BY
            ry.c_name
    </select>
    <select id="getTotal" parameterType="String" resultType="Integer">
        select
        sum(c_money)
        from purchase
        where
        t_date BETWEEN #{month}  and #{tomYear}
    </select>
    <select id="ylYsp" parameterType="String" resultType="Integer">
        select
        sum(xf.c_hymoney)-sum(sp.f_cost)
        from purchase xf,goods sp
        where
        xf.c_goodsId = sp.c_id
        and
        t_date BETWEEN #{month}  and #{tomYear}
    </select>
    <select id="ylWsp" parameterType="String" resultType="Integer">
        select
        sum(c_hymoney)
        from purchase
        where
        c_goodsId is null
        and
        t_date BETWEEN #{month}  and #{tomYear}
    </select>
</mapper>